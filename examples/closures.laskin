#!/usr/bin/env laskin

# Push list of strings into the stack.
["foo" "bar" "baz"]

# Push anonymous function into the stack.
:(string) {
    upper println
}

# Invoke function "each" on the list and function, which iterates over each
# value in the list and passes them to the function.
each
